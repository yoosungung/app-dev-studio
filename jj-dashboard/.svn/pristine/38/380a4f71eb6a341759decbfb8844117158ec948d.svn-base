package kr.ac.jj.dashboard.application.supercompetence.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;

/**
 * 교양교과 핵심역량 Controller
 *
 */
@Controller
@RequestMapping(path = "/supercompetence/SuperCompetence")
public class SuperCompetenceController {

    /**
     * 화면
     *
     * @return
     */
    @GetMapping
    public String view() {
        return "tiles2-common:/supercompetence/SuperCompetenceView";
    }

}
