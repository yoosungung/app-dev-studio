package kr.ac.jj.dashboard.application.dashboard.spread.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;

/**
 * 성적 Controller
 *
 */
@Controller
@RequestMapping(path = "/dashboard/spread/Spread")
public class SpreadController {

    /**
     * 화면
     *
     * @return
     */
    @GetMapping
    public String view() {
        return "tiles2-common:/dashboard/spread/SpreadView";
    }

}
