package kr.ac.jj.dashboard.application.dashboard.score.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;


/**
 * 성적 Controller
 *
 */
@Controller
@RequestMapping(path = "/dashboard/score/Score")
public class ScoreController {

    /**
     * 화면
     *
     * @return
     */
    @GetMapping
    public String view() {
        return "tiles2-common:/dashboard/score/ScoreView";
    }
}
